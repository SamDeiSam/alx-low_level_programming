#include "main.h"

/**
 * div - divides two integers and returns the result
 * @a: input two
 * @b: input two
 * Return: results after dividing two numbers
 */
int div(int a, int b)
{
	int c;
       
	c = a / b;

	return (c);
}
