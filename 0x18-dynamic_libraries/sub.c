#include "main.h"

/**
 * sub - subtracts two integers and returns the result
 * @a: int to be subtracted from
 * @b: int to subtract from a
 * Return: the results from subtracting b from a
 */
int sub(int a, int b)
{
	int c;

	c = a - b;

	return (c);
}
