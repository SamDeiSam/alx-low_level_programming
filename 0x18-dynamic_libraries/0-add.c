#include "main.h"

/**
 * add - add two numbers
 * @a: int to add to second number
 * @b: int to add to first number
 * Return: sum of two numbers
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
